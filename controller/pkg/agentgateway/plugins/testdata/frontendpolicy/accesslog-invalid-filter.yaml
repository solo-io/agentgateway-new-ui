apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayPolicy
metadata:
  name: accesslog-invalid-filter
  namespace: default
spec:
  targetRefs:
  - kind: Gateway
    name: test
    group: gateway.networking.k8s.io
  frontend:
    accessLog:
      filter: foolen_{{response.code}}

---
# Output
output:
- gateway:
    Name: test
    Namespace: default
  resource:
    policy:
      frontend:
        logging:
          filter: foolen_{{response.code}}
      key: frontend/default/accesslog-invalid-filter:frontend-logging:default/test
      name:
        kind: AgentgatewayPolicy
        name: accesslog-invalid-filter
        namespace: default
      target:
        gateway:
          name: test
          namespace: default
status:
- apiVersion: agentgateway.dev/v1alpha1
  kind: AgentgatewayPolicy
  metadata:
    name: accesslog-invalid-filter
    namespace: default
  spec: null
  status:
    ancestors:
    - ancestorRef:
        group: gateway.networking.k8s.io
        kind: Gateway
        name: test
        namespace: default
      conditions:
      - lastTransitionTime: fake
        message: 'frontend accessLog filter is not a valid CEL expression: foolen_{{response.code}}'
        reason: PartiallyValid
        status: "True"
        type: Accepted
      controllerName: agentgateway.dev/agentgateway
