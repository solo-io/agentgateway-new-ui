apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayPolicy
metadata:
  name: http-invalid-path
  namespace: default
spec:
  targetRefs:
  - kind: Gateway
    name: test
    group: gateway.networking.k8s.io
  traffic:
    extAuth:
      backendRef:
        name: extauthz
        port: 1024
      http:
        path: foolen_{{request.path}}
---
apiVersion: v1
kind: Service
metadata:
  name: extauthz
  namespace: default
spec:
  ports:
  - port: 1024

---
# Output
output:
- gateway:
    Name: test
    Namespace: default
  resource:
    policy:
      key: traffic/default/http-invalid-path:extauth:default/test
      name:
        kind: AgentgatewayPolicy
        name: http-invalid-path
        namespace: default
      target:
        gateway:
          name: test
          namespace: default
      traffic:
        extAuthz:
          http:
            path: foolen_{{request.path}}
          target:
            port: 1024
            service:
              hostname: extauthz.default.svc.cluster.local
              namespace: default
status:
- apiVersion: agentgateway.dev/v1alpha1
  kind: AgentgatewayPolicy
  metadata:
    name: http-invalid-path
    namespace: default
  spec: null
  status:
    ancestors:
    - ancestorRef:
        group: gateway.networking.k8s.io
        kind: Gateway
        name: test
        namespace: default
      conditions:
      - lastTransitionTime: fake
        message: 'extAuth http path is not a valid CEL expression: foolen_{{request.path}}'
        reason: PartiallyValid
        status: "True"
        type: Accepted
      controllerName: agentgateway.dev/agentgateway
